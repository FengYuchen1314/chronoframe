# 后台管理令牌。所有创建、上传、转换、删除操作均要求通过 X-Admin-Token 提供该值。
CF_ADMIN_TOKEN=change-this-before-production
CF_DATABASE_URL=sqlite://data/chronoframe.db?mode=rwc
CF_CONVERSION_WORKERS=4
# 可选：服务监听地址。
CF_BIND_ADDR=0.0.0.0:8080
# 构建后的 React 文件路径；开发时可不设置。
CF_WEB_DIR=./frontend/dist
