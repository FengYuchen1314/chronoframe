# docker-compose.yml 无需 .env 即可启动。本文件仅用于覆盖可选运行参数；
# 数据库、主密钥和本地图片固定保存在 Compose 同目录的 ./data。
CHRONOFRAME_BIND=0.0.0.0
CHRONOFRAME_PORT=8188
CF_CONVERSION_WORKERS=7
# 默认根据反向代理协议头自动决定 Secure；直连 HTTP 会保持非 Secure。
CF_COOKIE_SECURE=auto
# 默认兼容 HTTPS 反向代理，同时仍可直接使用 IP + 端口访问。
CF_TRUST_PROXY_HEADERS=true
